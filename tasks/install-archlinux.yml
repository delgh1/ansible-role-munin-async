---
- name: Ensure munin-node is installed because it includes munin-async (Arch Linux).
  pacman: name=munin-node state=present

- name: Create munin-async user on Arch Linux.
  user:
    name: "{{ munin_async_user }}"
    system: true
    home: "{{ munin_async_home }}"
